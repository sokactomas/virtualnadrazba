# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

APP_URL="http://localhost:3000"
WS_URL="ws://localhost:3001"

POSTGRES_USER=postgres
POSTGRES_PASSWORD=docker
POSTGRES_DB=postgres-db

# nest run in docker container
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=app&sslmode=prefer
# nest run locally
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?schema=app&sslmode=prefer
NODE_ENV='development'

# Auth
NEXTAUTH_SECRET='TtKhKGBuqKqvGG53dEM8fD23bOu+h1KEttkqPhiUeXE='
NEXTAUTH_URL='http://localhost:3000'
